services:
  mysql:
    image: mysql:8
    container_name: member-database
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    ports:
      - "${CONTAINER_DB_PORT}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - external-net

  member-service:
    image: mumulbo/member-service:dev
    container_name: member-service
    ports:
      - "${APPLICATION_PORT}:8082"
    env_file:
      - .env
    networks:
      - external-net
    depends_on:
      - mysql
    restart: unless-stopped

volumes:
  mysql_data:

networks:
  external-net:
    external: true
    name: backend-net
